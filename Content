sql :-  STRUCTURED QUERY LANGUAGE 
SQL- programing language used for run database.
    CRUD:-
        C- CREATE
        R-RETRIVE
        U-UPDATE
        D-DELETE

-- Types of sql  CAMMANDS:-
    there are four type of cammands in sql.
1. DDL  - Data Definition Language.
        1.1 CREATE - crate new database object.
        1.2 ALTER - modify existing database object.
        1.3 DROP - deleting existing database object.
        1.4 TRUCATE - remove all raws from table.
2. DML - Data Maniplation Language.
        1.1 INSERT - create new raw in tables.
        1.2 UPDATE - modify data in tables.
        1.3 DELETE - delete data from tables.
        1.4 SELECT - retrive data from tables.
3. DCL - Data Control Language.
        3.1 GRANT - provide access right on table.
        3.2 REVOKE - withdraw access right on database.
4.TCL - Transaction Control Language.
        4.1 COMMIT - save database changes and end transacion.
        4.2 ROLLBACK - undo changes that are not committed and end  transaction.
*/

-- datatypes in sql;
# Thier are too many datatypes in mysql --> 
-- DATATYPE                     DESCRIPTION                                        USAGE
-- CHAR.         string(0-255), can store characters of fixed length                 CHAR(50)
-- VARCHAR       string(0-255), can store characters up to given length              VARCHAR(50)
-- INT           integer(-2,147,483,648 to 2, 147,483,647 ) -- 4 bytes               INT
-- TINYINT        integer(-128 to 127)         #0 255                                TINYINT
-- BIGINT        integer(-9,223,372,036,854,775,808 to 9,223,372,036,854,775,807 )    BIGINT
--   FLOAT       Decimal number - with precision to 23 digits                         FLOAT
-- BOOLEAN       Boolean values 0 or 1                                                BOOLEAN
-- DATE          date in format of YYYY-MM-DD ranging from 1000-01-01 to 9999-12-31    DATE
-- YEAR          year in 4 digits format ranging from 1901 to 2155                     YEAR


-- char, varchar 0 to 255 bytes
-- 1 bytes == 1 alphabets
-- name char (40)     --  stste  varchar(40)
--  python              ---  Delhi
-- char used full bytes   &   -- varchar used only fill bytes used or relesd.
   
-- int ---> 9 digits  ---> 4 bytes (byedefeaut)
-- bigint ---> 10 digits
-- tinyint ---> -127 to 127
-- float ---> 

#STRING FUNCTIONS
-- UPPER()
-- LOWER()
-- REVERSE()
-- LEN()
-- REPLACE()
-- CONCAT()

#Commands in sql:-
-- 1. DDL(Data defination language)--Create,Drop,Alter,Truncate
-- 2. DQl(Data Query language)--Select
-- 3. DML(Data Manipulation langauge)--Insert,Update,Delete,Call
-- 4. DCL(Data Control Language)--Grant,Revoke
-- 5. TCL(Transaction Control Language)--Commit,Rollback,Savepoint


#Constraints:- 
- 1. not null
- 2. unique
- 3. auto_increament
- 4. check
- 5. default
- 6. primary key
- 7. foreign key

-- where clause
-- like operator
-- regex
-- groupby
-- orderby
-- limit
-- offset

#windows function:-
- 1, row_number
- 2. Rank
- 3. Dense_rank
#joins :-
* inner join
* outer join
* left join
* right join
* self join

-- nested query,
-- sub query
-- between,in
-- store procedure

